---
language: python
python:
  - "3.9"
services:
  - docker
install:
  - pip install molecule docker-py molecule-docker ansible
script:
  - molecule --version
  - ansible --version
  - molecule init scenario -r my_db_git -d docker
  - molecule create
  - molecule converge
  - molecule verify
  - molecule destroy
